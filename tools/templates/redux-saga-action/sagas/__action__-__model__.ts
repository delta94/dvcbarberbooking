import { __action__(pascalCase)__model__(pascalCase) } from '@/graphql/generated/graphql';
// import { __action__(camelCase)__model__(pascalCase)Api } from '@modules/__module__(pascalCase)/services/apis';
import { call, put } from 'redux-saga/effects';
import { __action__(camelCase)__model__(pascalCase)Error, __action__(camelCase)__model__(pascalCase)Success } from '../actions/__action__(kebabCase)-__model__(kebabCase)';
import { __action__(pascalCase)__model__(pascalCase)Action } from '../action-types';

export function* __action__(camelCase)__model__(pascalCase)Saga(action: __action__(pascalCase)__model__(pascalCase)Action) {
  // try {
  //   const res: __action__(pascalCase)__model__(pascalCase) = yield call(__action__(camelCase)__model__(pascalCase)Api, action.payload);
  //   yield put(__action__(camelCase)__model__(pascalCase)Success(res));
  // } catch (error) {
  //   yield put(__action__(camelCase)__model__(pascalCase)Error(error));
  // }
}
